---
# tasks file for app
- name: Show info about the env this host belongs to
  debug:
    msg: "This host is in {{ env }} environment!!!"

- include: pg_install.yml
- include: pg_config.yml
- include: pg_users.yml
